export interface Project {
  id: string;
  name: string;
  description: string;
  categories: string[];
  date: string;
  heroImage: string;
  pdf: string;
}

export interface Experience {
  id: string;
  title: string;
  description: string;
}
